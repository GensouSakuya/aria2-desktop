<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:GensouSakuya.Aria2.Desktop.Shell.Controls.ViewModels"
             mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="50"
             x:Class="GensouSakuya.Aria2.Desktop.Shell.Controls.DownloadTaskItem">
  <UserControl.DataContext>
    <vm:DownloadTaskItemViewModel></vm:DownloadTaskItemViewModel>
  </UserControl.DataContext>
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto"></ColumnDefinition>
      <ColumnDefinition Width="*"></ColumnDefinition>
    </Grid.ColumnDefinitions>
    <Image Grid.Column="0" Source="{Binding Img}" Width="50" Height="50"></Image>
    <DockPanel Grid.Column="1" VerticalAlignment="Center" LastChildFill="True" Margin="10 0 20 0">
      <DockPanel DockPanel.Dock="Top" LastChildFill="False">
        <TextBlock Text="{Binding TaskName}" DockPanel.Dock="Left"></TextBlock>
        <TextBlock Text="{Binding DownloadSpeedStr}" DockPanel.Dock="Right"></TextBlock>
      </DockPanel>
      <ProgressBar Minimum="0" Maximum="100" Value="{Binding Progress}" Height="10" VerticalAlignment="Bottom"></ProgressBar>
    </DockPanel>
  </Grid>
</UserControl>
